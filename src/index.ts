#!/usr/bin/env node

import { program } from 'commander'

(() => {
  program
    .description('"D"isk "C"atalog with SQ"Lite" backend')

  // const parsed = program.parse(process.argv)
  // const args = parsed.opts()


  // print "test" to stdout
  console.log('test')
})()
